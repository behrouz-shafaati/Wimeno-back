import c_service from "../../core/service";

export default class shopService extends c_service {
  /**
   * constructor function for shopService.
   *
   * @remarks
   * This method is part of the shopService class extended of the main parent class baseService.
   *
   * @param model - shopSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
