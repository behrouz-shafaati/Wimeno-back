import c_service from "../../core/service";

export default class tabelService extends c_service {
  /**
   * constructor function for tabelService.
   *
   * @remarks
   * This method is part of the tabelService class extended of the main parent class baseService.
   *
   * @param model - tabelSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
