import c_service from "../../core/service";

export default class productService extends c_service {
  /**
   * constructor function for productService.
   *
   * @remarks
   * This method is part of the productService class extended of the main parent class baseService.
   *
   * @param model - productSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
